const CreateTree = (array) => {

    let _root = null;

    function buildTree(array) {

        array.sort((a, b) => a - b);
        console.log(array);
        
       // return rootNode;
    }

    buildTree(array);

}

export { CreateTree };